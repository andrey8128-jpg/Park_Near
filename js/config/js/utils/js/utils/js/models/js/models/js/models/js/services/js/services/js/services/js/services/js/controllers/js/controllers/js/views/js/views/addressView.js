export function renderAddressList(addresses) {
    // формирует список адресов с метками и кнопкой "Найти парковку"
}

export function renderAddressForm() {
    // форма добавления адреса (с переключателем ручной/карта)
}

export function renderFavorites(content) {
    // загружает адреса и избранные парковки
}
