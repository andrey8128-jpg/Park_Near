export function renderEditPanel(data, isFavorite) {
    // генерирует HTML для редактирования парковки
    // возвращает строку
}

export function renderParkingSheet(id, data) {
    // отображает нижний лист с информацией и кнопкой "Маршрут"
}
